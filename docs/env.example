# Copy to `.env` if you want local tooling (Prisma Studio, scripts) to target a specific DB.
# NOTE: local Docker runs the API with env values from `docker-compose.yml`.

# App
NODE_ENV=development
PORT=3000
BASE_URL=http://localhost:3000
SWAGGER_ENABLED=true

# Database (Postgres + PostGIS required)
DATABASE_URL=postgresql://betourist:betourist@localhost:5433/betourist?schema=public

# Auth
JWT_ACCESS_SECRET=change-me
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_SECRET=change-me-too
JWT_REFRESH_EXPIRES_IN=30d

# CORS (comma-separated origins)
# Examples:
# - CORS_ORIGINS=http://localhost:5173
# - CORS_ORIGINS=https://your-frontend.app
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000

# Rate limiting (per-IP)
THROTTLE_TTL_SECONDS=60
THROTTLE_LIMIT=120

# Proxy settings (set true behind Railway / load balancers)
TRUST_PROXY=true


